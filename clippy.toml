# ============================================================
# clippy.toml — NeroShizaDev-OS
# Цель: молчать на стилистику, кричать только на реальные баги.
# ============================================================

# Снижаем шум по lint `too_many_arguments` для системных функций ядра
too-many-arguments-threshold = 12

# Разрешаем типы-пустышки без полей (маркеры архитектуры)
# cognitive-complexity-threshold = 30   # (если нужно — раскомментировать)

