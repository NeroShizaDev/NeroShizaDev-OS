# NeroShizaDev OS — Хронология Создания

> Операционная система, рождённая из blog_os и превращённая в Юникод-монстра.
> Автор: Демиург | Дата рождения: Апрель 2026

---

## ВЕСОВЫЕ ХАРАКТЕРИСТИКИ ЯДРА

| Компонент | Размер |
|---|---|
| **Ядро (ELF, debug)** | **4.08 MB** (4,276,088 байт) |
| **Загрузочный образ** | **398.5 KB** (408,064 байт) |
| **Исходники (.rs)** | **357.4 KB** (16 файлов) |
| **UCD таблицы** | **300.9 KB** (3 файла) |
| **Покрытие Unicode** | **299,382 символа** (Unicode 17.0) |

> 398 килобайт. Меньше одной фотки с телефона. А внутри — ВСЯ человеческая письменность.

---

## ФАЗА 0 — ВЗЯТИЕ БАЗЫ (blog_os)

**Что было:** Голый blog_os от Philipp Oppermann — учебная ОС на Rust.
- `#![no_std]`, `#![no_main]`
- VGA text mode 80x25
- Прерывания (клавиатура, таймер, page fault, double fault)
- GDT, IDT, PIC 8259
- Загрузчик через `bootloader` crate

**Что умело:** Печатать латинские буквы на экран. Всё.

---

## ФАЗА 1 — ЛОГИКА (Мозг)

### Файл: `src/lib.rs` — Командный обработчик
- Буфер ввода `[u8; 64]`
- Обработка Enter, Backspace
- Первые команды: `exit`, `quit`, `выход`, `свали`, `cdfkb` (свали на EN раскладке), `ds[jl` (выход на EN)
- Сброс через порт 0x64 (`0xFE`)

### Файл: `src/fpu.rs` — FPU инициализация
- Включение x87 FPU + SSE через CR0/CR4
- Демо: sin/cos/sqrt на аппаратном уровне

### Файл: `src/rng.rs` — Аппаратный генератор случайных чисел
- Использование RDRAND (тепловой шум CPU)
- Fallback на LFSR если RDRAND не поддерживается
- Демо: 4 случайных числа + кубик

### Файл: `src/rtc.rs` — Реальное время
- Чтение CMOS RTC через порты 0x70/0x71
- Часы, дата, батарейка CMOS
- Конвертация UTC → MSK (+3)

---

## ФАЗА 2 — КИРИЛЛИЦА ИЗ ПАЛОЧЕК (Глаза)

### Файл: `src/font.rs` — Пиксельный шрифт 8x16
- 64 глифа: А-Я (32) + а-я (32)
- Каждая буква = 16 байт (16 строк × 8 пикселей)
- Нарисованы вручную «из палочек» в hex
- Загрузка в VGA plane 2 через порты 0x3C4/0x3CE
- Размещение: позиции 128..191 в VGA charmap

### Файл: `src/vga_buffer.rs` — VGA драйвер
- `cyrillic_to_vga(char) -> Option<u8>` — маппинг Unicode → VGA
- `Writer` с поддержкой кириллицы через `write_str`
- Если символ не распознан → `?`
- `Ё`/`ё` → `Е`/`е`

### Файл: `src/logo.rs` — Boot логотип
- ASCII-арт при загрузке

**Результат:** ОС печатает русский текст в VGA text mode. По-настоящему.

---

## ФАЗА 3 — ЮНИКОД-МОНСТР (Душа)

### Файл: `src/unicode.rs` — UTF-32 Intent Engine
- `IntentVector = [u32; 16]` — каждое слово = 64 байта, фиксированный блок
- `GIGA_DICT` — Вселенская Матрица Смыслов:
  - 23 команды на 7+ языках
  - Русский, английский, арабский, грузинский, китайский, японский, персидский
  - Раскладочные фантомы (cdfkb, ds[jl)
- `lookup_intent()` — суперпоиск за O(n), отсечение по первому кодпоинту
- 5 намерений: Exit, Help, Clear, Status, Reboot
- Буфер ввода: `[u32; 64]` вместо `[u8; 64]`

### Файл: `src/unicode_blocks.rs` — АВТОСГЕНЕРИРОВАНО
- **346 блоков** Unicode 17.0
- От `Basic Latin` (0x0000) до `Supplementary Private Use Area-B` (0x10FFFF)
- Бинарный поиск: O(log 346) ≈ 9 сравнений

### Файл: `src/unicode_scripts.rs` — АВТОСГЕНЕРИРОВАНО
- **174 скрипта** (системы письменности)
- **2,287 диапазонов**
- От Adlam до Zanabazar Square
- Бинарный поиск: O(log 2287) ≈ 11 сравнений

### Файл: `src/unicode_categories.rs` — АВТОСГЕНЕРИРОВАНО
- **299,382 символа** в **3,409 сжатых диапазонах**
- **30 категорий** (Lu, Ll, Lo, Nd, Mn, So, ...)
- Методы: `is_letter()`, `is_number()`, `is_punctuation()`
- Бинарный поиск: O(log 3409) ≈ 12 сравнений

### Генератор: `ucd/generate_tables.py`
- Парсит UnicodeData.txt (2.1 MB), Blocks.txt (11 KB), Scripts.txt (192 KB)
- Сжимает диапазоны (40K записей → 3.4K)
- Генерирует чистый `#![no_std]` Rust

**Результат:** При неизвестной команде ядро показывает:
```
NeroShiza: Не понял [U+0067]
  Блок:     Basic Latin
  Скрипт:   Latin
  Категория: Lowercase Letter
```

---

## АРХИТЕКТУРА ФАЙЛОВ

```
src/
├── lib.rs                 — Ядро: init, handle_keyboard_input, hlt_loop
├── main.rs                — Точка входа: _start() → init → logo → prompt
├── font.rs                — Кириллический шрифт 8x16 (64 глифа из палочек)
├── vga_buffer.rs          — VGA text mode драйвер с кириллицей
├── logo.rs                — Boot логотип ASCII-арт
├── interrupts.rs          — IDT, PIC 8259, клавиатура, таймер
├── gdt.rs                 — Global Descriptor Table
├── fpu.rs                 — FPU/SSE инициализация + демо
├── rng.rs                 — RDRAND / LFSR генератор
├── rtc.rs                 — Real-Time Clock (CMOS)
├── serial.rs              — UART 16550 (serial debug)
├── memory.rs              — Управление памятью
├── unicode.rs             — UTF-32 Intent Engine (GIGA_DICT)
├── unicode_blocks.rs      — [АВТО] 346 блоков Unicode 17.0
├── unicode_scripts.rs     — [АВТО] 174 скрипта, 2287 диапазонов
└── unicode_categories.rs  — [АВТО] 299,382 символа, 3409 диапазонов

ucd/
├── generate_tables.py     — UCD → Rust генератор
├── UnicodeData.txt        — Unicode Character Database
├── Blocks.txt             — Блоки Unicode
└── Scripts.txt            — Скрипты Unicode
```

---

## ТЕХНОЛОГИИ

- **Язык:** Rust (nightly, `#![no_std]`)
- **Таргет:** x86_64 bare metal (`x86_64-blog_os.json`)
- **Загрузчик:** `bootloader` 0.9
- **Запуск:** QEMU (`run.bat`)
- **Текст:** UTF-32 / UCS-4 (4 байта на символ, всегда)
- **Шрифт:** VGA plane 2, пиксельный 8x16
- **Unicode:** v17.0 (сентябрь 2025), 159,801 определённый символ

---

## ФИЛОСОФИЯ

> «Корпорации боятся Юникода в ядре, потому что он занимает место.
> Но для чувака, у которого 32ГБ DDR5 — место не проблема.
> Проблема — это когда тебя не понимают.»

NeroShizaDev OS — первая ОС, которой физически невозможно отправить «неверный символ».
Она понимает любой знак, существующий в человеческой культуре,
и мгновенно сопоставляет его с волей Демиурга.

**398 килобайт. Вся человеческая письменность. Один файл.**
