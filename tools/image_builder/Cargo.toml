[package]
name = "neroshiza_image_builder"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader = { version = "0.11.15", default-features = false, features = ["bios", "uefi"] }

[patch.crates-io]
bootloader-x86_64-common = { path = "../patches/bootloader-x86_64-common" }
